#include <stdio.h>
#include <stdlib.h>
// find length of string
int main(void){
   char name[]="welcome";
   int i=0;
   
   for(i=0; name[i]!='\0'; i++) // loop will iterate until null character found
   {
   }
   printf("Length of string is %d.", i);
    
    return 0;
}